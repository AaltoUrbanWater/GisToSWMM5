# Initial key string
<<< Link c6 >>>
# Number of lines to be skipped after the key string line
4
# Extracted series column number (0 is the first column)
0
# Series length in lines (-1 reads the whole series)
-1
# Series stop string (if -1 is specified above). Enter \n characters if a newline is required.
  \n
# Series name
date
